name: golfbuana_flutter
description: GolfBuana — Community Golf Platform (Flutter)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.0
  go_router: ^13.0.0
  flutter_riverpod: ^2.4.0
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  intl: ^0.19.0
  url_launcher: ^6.2.4
  share_plus: ^7.2.1
  flutter_dotenv: ^5.1.0
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
